# built bundles, except the example the README tells you to open
decks/*.json
!decks/example.json
!decks/isd2026.json

# test artefacts
tests/fixture.json

# local
.venv/
__pycache__/
*.pyc
.DS_Store

# the Vercel link, which is per-machine
demo/vercel/.vercel/

# Keys. .env.example is the template and is committed; .env never is.
.env
.env.*
!.env.example
