name = "slaidy-demo-ai"
main = "worker.js"
compatibility_date = "2025-01-01"

# Optional but recommended: the rate limits need somewhere to count.
# Create it once with:  npx wrangler kv namespace create LIMITS
# then paste the id here and uncomment.
#
# [[kv_namespaces]]
# binding = "LIMITS"
# id = "…"
